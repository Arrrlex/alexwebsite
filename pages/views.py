# from django.shortcuts import render
from django.http import HttpResponse

# def index(request, current_page):
#     return render(request, 'pages/index.html', {'current_page': current_page})

# def cv_index(request, current_page):
# 	return render(request, 'pages/cv.html', {'current_page': current_page})

# def contact(request, current_page):
# 	return render(request, 'pages/contact.html', {'current_page': current_page})
